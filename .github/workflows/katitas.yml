name: Check Katitas property count

on:
  schedule:
    # 17:55 JST = 08:55 UTC. Run a few minutes before the 18:00 notification.
    - cron: "55 8 * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Check current property count
        run: python katitas.py

      - name: Upload screenshots and page text
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: katitas-${{ github.run_id }}
          path: artifacts/
          if-no-files-found: ignore
          retention-days: 14
